#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define ascend(A) sort(A.begin(),A.end())
#define input(A,N) for(ll i=0;i<N;i++) scanf("%lld",&A[i]);
void solve()
{
    ll N;
    cin>>N;
    vector<ll> A(N);
    input(A,N);
    ascend(A);
    vector<ll> B,C;
    for()
    {

    }
}
int main()
{
    solve();
    return 0;
}